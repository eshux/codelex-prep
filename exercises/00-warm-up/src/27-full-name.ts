export {};

/**
 * Create a function called fullName, which concatenates two strings together
 */
const fullName = (name, surname) => name.concat(' ').concat(surname);

console.log(fullName("John", "Doe")); //Expected output: 'John Doe'
